# Back_up-V2.00-
I built a Python automation system that performs file backup operations using a modular architecture. The system reads files from a source directory and creates timestamped backups while logging every operation.
